{
  "name": "sex-technology-site",
  "private": true,
  "type": "module",
  "scripts": {
    "images:optimize": "node scripts/optimize-images.mjs",
    "images:optimize:all": "node scripts/optimize-images.mjs --all",
    "images:optimize:dry": "node scripts/optimize-images.mjs --dry-run"
  },
  "devDependencies": {
    "sharp": "^0.34.5"
  }
}
